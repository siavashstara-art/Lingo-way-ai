# Server Configuration
PORT=3000
NODE_ENV=development

# Gemini AI API Key (Set on Server-side ONLY - Never expose in frontend client)
# In production on Google Cloud / AI Studio, this is injected automatically.
GEMINI_API_KEY=
